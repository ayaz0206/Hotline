<%= form_for @question do |f| %>

	<% if flash[:errors] %>
		<% flash[:errors].each do |errors| %>
			<%= errors %>
		<% end %> 
	<% end %>
	
	<%= f.text_field :title %> <br>
	<%= f.text_area :description %> <br>
	<div id="placeholder-div3"></div> <br>

	<%= f.submit "Ask" %>
<% end %>